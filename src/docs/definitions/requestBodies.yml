definitions:
  requestBodies:
    Login:
      type: object
      properties:
        email:
          type: string
        password:
          type: string
    Register:
      allOf:
        - $ref: "#/definitions/requestBodies/Login"
        - properties:
            fullName:
              type: string
